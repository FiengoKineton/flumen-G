dynamics:
  name: VanDerPol
  args:
    damping: 1.0
sequence_generator:
  name: GaussianSqWave
  args:
    period: 5           # for GaussianSqWave
    # max_freq: 2.0       # for SinusoidalSequence | (Nyquist-Shannon sampling theorem) f_max < f_s / 2 = 1 / (2 * control_delta) = 2.5Hz

control_delta: 0.2